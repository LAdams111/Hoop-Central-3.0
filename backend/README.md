# Backend for Basketball App

This is the backend of the basketball app, built using Node.js and Express.

## Installation

1. Navigate to the backend directory.
2. Run `npm install` to install dependencies.

## Running the Server

Run `node server.js` to start the server.
